# App-level release rules are intentionally kept empty.
# All concrete keep rules live in each library module's consumer-rules.pro so they
# travel with the module and do not need to be duplicated here.
